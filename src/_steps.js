// Steps
/*
Steps to use firebase
    1. Create a project on console.firebase.google.com
    2. Install Firebase $npm install firebase
    3. Register web app in firebase
    4. Copy fire base init with config from firebase settings into a file firebase.init.js
    5. export firebase app and go authentication setting and set a method
    6. import getAuth form firebase/auth in app.js and crate auth = getAuth(app)
    7. import app firebase init.js into your app.js
    8. Call Google auth provider
    9. call signInWithPopUp and pass two peremeter like auth and provider
    10. handle then (if successful) and catch (if error)
*/